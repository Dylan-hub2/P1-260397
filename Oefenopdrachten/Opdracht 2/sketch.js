function setup() {
  createCanvas(400, 400);

}

function draw() {
  background(220);
  // stap 1
  let x = 100;
  text(x, 20, 20)

  // stap 2 
  let groet = "hello world"
  text(groet, 20, 60);

  // stap 3 + 4
  let a = 20;
  let b = 20;

  let optellen = a + b;
  text ("optellen: " + optellen, 20, 70)
  let aftrekken = a - b;
  text("aftrekken: " + aftrekken, 20 ,80)
  let delen = a/b 
  text("delen: " + delen, 20 ,90)
  let
}
